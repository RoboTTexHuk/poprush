workflows:
  ios-workflow:
    name: iOS Build Workflow
    environment:
      flutter: stable
      vars:
        APPLE_DEVELOPER_CREDENTIALS: Encrypted(...)
    scripts:
      - name: Install dependencies
        script: flutter pub get

      - name: Build iOS IPA
        script: flutter build ipa --release